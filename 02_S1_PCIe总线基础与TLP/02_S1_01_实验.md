---
created: 2026-04-17
modified: 2026-04-17
type: learn
lifecycle_status: active
summary: S1_01实验手册：PCIe设备扫描、TLP类型识别、链路状态读取，共4个实验。
tags:
  - type/learn
  - Linux/PCIe驱动
  - PCIe协议
  - TLP
  - S1
  - 实验
---

# S1_01_实验 — PCIe 总线基础与 TLP

> **⚠️ 编译注意**：课程代码必须放在 home 目录下编译（不要放在 NTFS 分区，chmod +x 在 NTFS 上不生效）
> 代码仓库路径：`~/work/code/PCIe_driver_learning/`
> 
> 前提：安装 libpci-dev
> `sudo apt install libpci-dev`

---

## 实验 1：PCIe 总线扫描

**目标**：用 scan 工具扫描宿主机 PCIe 总线，理解拓扑结构。

**步骤**：
```bash
# 编译
make scan

# 扫描所有 PCIe 设备
sudo ./S1_01_pci_scan

# 对比 lspci
lspci -nn

# 过滤 PCIe 设备
lspci -nn | grep PCIe
```

**预期结果**：00:00.0 是 RC（Host Bridge），其他是 EP 或 Switch 端口。

**验收标准**：
- [ ] scan 工具正常运行
- [ ] 能区分 Type 0（EP）和 Type 1（RC/Bridge）

---

## 实验 2：TLP 类型识别

**目标**：用 tlp_type 工具识别 RC 和 EP 的 Device Type，理解 TLP 角色。

**步骤**：
```bash
# 编译
make tlp_type

# 分析 RC（00:00.0）
sudo ./S1_01_tlp_type 00:00.0

# 分析第一个 EP（通常 01:00.0 virtio-net 或 nvme）
sudo ./S1_01_tlp_type 01:00.0

# 列出所有 PCIe 设备
lspci -nn | awk '{print $1}' | while read dev; do
    echo "=== $dev ===" && sudo ./S1_01_tlp_type $dev 2>/dev/null || true
done
```

**验收标准**：
- [ ] RC 的 Device Type = Root Port
- [ ] EP 的 Device Type = PCIe Endpoint
- [ ] 能解释 RC 发给 EP 的 TLP 类型

---

## 实验 3：链路状态读取

**目标**：读取链路协商速率和宽度，理解 Gen/Width 协商过程。

**步骤**：
```bash
# 编译
make link_status

# 读取 RC 链路状态
sudo ./S1_01_link_status 00:00.0

# 读取 EP 链路状态
sudo ./S1_01_link_status 01:00.0

# 用 lspci 交叉验证
lspci -vv -nn -s 00:00.0 | grep -i "lnksta\|speed\|width"
```

**验收标准**：
- [ ] 能读出 Gen1/2/3/4/5 速率
- [ ] 能读出 x1/x2/x4/x8/x16 宽度
- [ ] 理解协商宽度受链路质量限制

---

## 实验 4：TLP 路由验证（可选）

**目标**：用 setpci 触发配置访问，观察 CfgRd/CfgWr 的 ID 路由行为。

**步骤**：
```bash
# 读取 RC 的 Vendor/Device ID（CfgRd → ID 路由）
sudo ./S1_01_tlp_type 00:00.0 | grep "Vendor ID"

# 对比：直接用 setpci 读（CfgRd）
sudo setpci -s 00:00.0 0.w

# 读 EP 的 BAR0（通过配置空间）
sudo setpci -s 01:00.0 10.l

# 读 MSI Cap Message Data（EP 发 MSI 时写入的数据）
sudo ./S1_01_tlp_type 01:00.0 | grep "MSI"
```

**验收标准**：
- [ ] 能用 setpci 读取配置寄存器
- [ ] 理解 ID 路由不依赖地址，只依赖 Bus/Dev/Func
