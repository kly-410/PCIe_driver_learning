---
created: 2026-04-18
modified: 2026-04-18
type: learn
lifecycle_status: draft
summary: S1_02实验手册：链路状态读取/FC分析/DOE验证，共3个实验。
tags:
  - type/learn
  - S1
  - 实验
---

# S1_02_实验 — 链路层与 Flow Control + DOE

> 本节实验较难直接接触硬件层，重点在通过工具观察链路状态和理解 Flow Control 概念。

---

## 实验 1：链路状态深度分析

**目标**：通过 `lspci -vv` 读取链路详细信息，理解 Flow Control 协商结果。

**步骤**：
```bash
### 读取 RC（00:00.0）的链路状态
sudo lspci -vv -nn -s 00:00.0 | grep -E "LnkSta|LnkCtl|FC"

### 读取第一个 EP 的链路状态
sudo lspci -vv -nn -s 01:00.0 | grep -E "LnkSta|LnkCtl|FC"

### 完整输出（方便整体分析）
sudo lspci -vv -nn -s 00:00.0
sudo lspci -vv -nn -s 01:00.0
```

**预期结果**：
- `LnkSta`：显示当前协商速率（Gen1/2/3）和宽度（x1/x2/x4）
- `LnkCtl`：显示 Flow Control 状态
- 能看到 RC 和 EP 的协商结果是否一致

**验收标准**：
- [ ] 能读出 RC 和 EP 的链路状态
- [ ] 能解释为什么两个设备协商结果应该一致

---

## 实验 2：PCIe AER 错误状态读取

**目标**：通过 AER（Advanced Error Reporting）读取链路错误，理解数据链路层的错误检测机制。

**步骤**：
```bash
### 检查 AER 状态寄存器
sudo lspci -vv -nn -s 00:00.0 | grep -E "Uncorrectable Error|Correctable Error|AER"

### 检查 AER Uncorrectable Error Status
sudo setpci -s 00:00.0 AER_UE_STATUS.w

### 检查 AER Correctable Error Status
sudo setpci -s 00:00.0 AER_CE_STATUS.w
```

> 注：AER 寄存器地址需要通过 PCIe Capability 结构找到偏移量。

**预期结果**：正常情况下所有错误状态位为 0（无错误）

**验收标准**：
- [ ] 能理解 AER 错误状态位的含义
- [ ] 确认当前链路无错误（所有状态位为 0）

---

## 实验 3：DOE 基本操作（如果有硬件支持）

**目标**：如果有 DOE 能力（查看 PCIe Capability 的 VSEC 区域），尝试读取 DOE 对象。

**步骤**：
```bash
cd ~/work/code/PCIe_driver_learning/03_S1_链路层与FlowControl_DOE

### 编译 DOE 程序
make doe

### 查看所有设备的 VSEC 和 DOE 能力
### （如果没有 DOE 硬件，程序会报告 "No DOE support"，这是正常的）
for dev in $(lspci | awk '{print $1}'); do
    echo "=== $dev ==="
    sudo ./S1_02_doe $dev 2>/dev/null || true
done
```

**说明**：如果没有 DOE 设备，程序会报告 "No DOE support"，这是正常的。

**预期结果**：能识别哪些设备有 DOE 能力

**验收标准**：
- [ ] 理解 DOE 的作用（读/写配置对象）
- [ ] 知道 DOE 不依赖 TLP，靠数据链路层传输

---

## 附：关键寄存器速查

| 寄存器 | 作用 |
|--------|------|
| `LnkSta` | 当前链路速率/宽度/状态 |
| `LnkCtl` | 链路控制（FC 启用/禁用） |
| `AER_UE_STATUS` | 不可纠正错误状态 |
| `AER_CE_STATUS` | 可纠正错误状态（CRC/NAK） |
